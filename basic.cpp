// iosStream hold inputs and outputs operations.. and a little more.
#include <iostream>

int main(){
    // std means standard.
    // cout >> the "c" means character, and the fallowing "out" means output. 
    // standard character output.      endl means endLine. 
    std::cout << "I like Pudim!" << std::endl;
    std::cout << "It's too good to be true" << std::endl;

    // there's another way to separate the lines. it is by using: '\n'  

    std::cout << "I will achieve everything." << '\n';
    std::cout << "I'll make sure about it." << '\n';









    //the use of return 0:  it is because if the program return 0, there's nothing wrong with our program, but if it return 1. there's a problem in it. 
    return 0;
}

